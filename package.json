{
  "name": "bwithu-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:shared": "npm run build -w packages/shared && npm run build -w packages/ui",
    "dev:chrome": "npm run dev -w apps/chrome-extension",
    "build:chrome": "npm run build:shared && npm run build -w apps/chrome-extension",
    "dev:mac": "npm run dev -w apps/macdesktop",
    "build:mac": "npm run build:shared && npm run build -w apps/macdesktop",
    "dev:windows": "npm run dev -w apps/windowsdesktop",
    "build:windows": "npm run build:shared && npm run build -w apps/windowsdesktop"
  },
  "devDependencies": {
    "typescript": "~6.0.2"
  }
}
